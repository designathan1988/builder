Read CLAUDE.md, .memory/builder.md, and .memory/audit-checklist.md before work. The authoritative user instruction is the full literal prompt in .memory/builder-brief.md; it replaces prior Codex workflow prompts. Earlier audit criteria and adenda are preserved in .memory/builder-audit-source.md until handoff supplies the canonical brief.

Work on branch codex in C:\Users\jonathanrodriguesti\Documents\builder-codex. Only write there and C:\Users\jonathanrodriguesti\Documents\builder-coord\BOARD.md. Never write in C:\Users\jonathanrodriguesti\Documents\builder. No subagents. English in project files; Portuguese only in BOARD and user messages.

You are the sole builder, integrator, test runner and main-branch advancer. Finish 7.1d and 7.1e, then the handoff and the ordered blocks in .memory/builder-brief.md. At the start of the next item, read the Claude handoff once BOARD confirms it; merge frozen origin/integration into codex and preserve both sides. Commit and push each part on codex. Advance main only with git push origin codex:main after complete verify:fast and e2e pass at that exact commit. Never force push or skip hooks.

Keep PORT=5341 npm run dev running. Real use is ONLY in the Codex in-app browser at http://localhost:5341, alongside implementation. Never open a visible Chrome, Edge or default-browser window on the user's computer. Playwright tests and ZIP inspection scripts run headless only; .cache/scratch/uso-lib.mjs must stay headless. No browser lock or file reservation remains. Use E2E_PORT=5311; light E2E uses E2E_WORKERS=2, complete block-end suite uses default workers. One Playwright process at a time.

Each part needs a manifest/spec contract, specific scenarios, targeted typecheck/lint/unit/E2E, a feature-off tooth, and measured in-app browser use at Desktop 100% and Phone Fit with evidence in .cache/logs/uso-<part>-<time>/. Export and inspect ZIP headlessly when output changes. Never weaken existing tests, scenarios or fixtures. A committed scenario that needs correction is retired with a documented before/after and replaced by one at least as strong. Keep messages to one delivery line per part; if commit or push is blocked, report immediately.
